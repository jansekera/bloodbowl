# CO SE ZMĚNILO 12.09.2026 — SOUHRN DNE

22 commitů, **1041 testů zeleně**, `main == origin/main`.

## 1. KLEC (`PHP33`) — nová sekce, zadání uživatele

Klec = **nosič + čtyři DIAGONÁLNÍ rohy**. Ortogonální soused je k ničemu.

| co | commit |
|---|---|
| roh `+1,5` místo ploché sousednosti *(dřív vznikala hvězda, ne klec)*; kdo v rohu **stojí, zůstává stát** (`STAND_PAT`) | `5f10ece2` |
| nosič **neuteče vlastní kleci** | `463aea16` |
| strop posunu = **nejmenší MA z pěti**, ne natvrdo jedno pole | `77bf8fb3` |
| nejpomalejší klec **nezastaví — dožene ji přes GFI**, ale až na konci kola | `0cbb2ec9` |
| **špinavý roh** = náš hráč se soupeřem vedle; nosič **nesmí skončit vedle soupeře** | `79af0a88` |
| **soupeřův roh je horší než prázdný** — *block vs blitz* | `34e0889f` |
| čtyři zadní hráči = **posádka klece**, rozestavení ke středu | `5a3215bf` |
| posádku **něco táhne k rohům** (klesající bonus), zvednutí míče má přednost | `4e6ba648` |
| **nekrytý míč = nejvyšší priorita**, a jde pro něj specialista | `a6ce3bb9` |
| **čistý roh se preferuje před špinavým** | `19816f91` |

## 2. RIZIKO — největší oprava dne

⛔ `dodges * 0,15 + gfis * 0,08` byl **paušál**: dodge za **33 %** *(trpaslík
AG 2)* stál stejně jako za **67 %** *(elf AG 4)*. ⭐ A `successChance` bylo
v nabídce **celou dobu**.

⇒ Pokuta podle skutečné šance, u **nosiče** těžší váha *(cena selhání je
turnover)*, a **pod 50 % tvrdá hranice** — tam už se takový tah nehraje.
`bf232f58`, přeneseno i do `GreedyAICoach` (`d1f501f8`).

A **zvednutí míče** se od `c72b46cd` oceňuje **podle šance toho, kdo zvedá** —
práh se bere z `BallResolver::getPickupTarget()`, tedy z téže funkce, kterou
pak použije engine.

## 3. PRAVIDLOVÉ OPRAVY

- **`PHP32` — dovednost Kick byla špatně dvakrát** *(`8b943d28`)*: zaokrouhlovalo
  se **nahoru** místo dolů *(r. 8213: „1 = 0")* a **nehlídalo se, kde kopající
  stojí** *(široký pás, lajna)*. ⛔ Existující test tu první vadu **zakotvoval**.
- **`touchback` je nedosažitelný** *(`b7b40aaa`)*: kope se pořád na **konstantu**
  — střed přijímací poloviny. Změřeno: 200 výkopů na `x=6` ⇒ 0 touchbacků,
  200 na `x=19` ⇒ 200. Chybí **volba, kam kopnout**. ⇒ `PHP31`.
- **`take_root` je v pořádku** — nula byla vzorkování *(jediná rasa)*. ⛔ Ale
  vyšlo najevo, že **Wood Elf roster nemá Treemana**. ⇒ `PHP30`.

## 4. MĚŘIDLA

- **histogram typů událostí** + seznam těch, které **nenastaly ani jednou**
  *(37 z 82, projito jedno po druhém)*. ⛔ Nalezena jedna skutečná díra:
  **`GameEvent::sneakyGit()` se nevolá nikde v `src/`**.
- **měřidlo klece** — čtyři třídy rohů, uživatelovo pořadí závažnosti,
  rozpad „bez nosiče" a **„co kouč zvolil místo zvednutí"**.

## 5. ⛔⛔ TŘI VLASTNÍ CHYBY, KTERÉ MĚŘENÍ ODHALILO

1. **Histogram nesbíral události z `GameFlowResolver`** ⇒ `touchdown` a `kickoff`
   hlásil jako nulu. Vypadalo to jako nález o enginu.
2. **Kontrola rosterů porovnávala hodnoty enumu proti jménům case** ⇒ tvrdila,
   že `ThrowTeamMate` v rosterech není, ačkoli ho má goblinní Troll.
3. **Špatný popisek koše**: *„míč volný — nikdo ho nezvedl"* měřilo stav
   **na začátku kola**. Doměření: kouč pro míč šel **56 z 56×**; jen mu padal hod.

⇒ Ke každé nule i jedničce patří otázka, **co přesně měřidlo počítá**.

## 6. ČÍSLA

| | ráno | po opravě rizika | večer |
|---|---|---|---|
| kol končí turnoverem | **96,2 %** | 70,5 % | **62,0 %** |
| `move → player_fell` z turnoverů | **64,8 %** | ~16 % | ~11 % |
| konec „pojistkou" *(mimo katalog)* | 0 | 0 | **0** |
| čtyři čisté rohy | — | 0 % | **9,1 %** *(11 kol)* |

⚠️ Srovnání **napříč časem**, ne párové A/B.

## ⏰ CO ZBÝVÁ

1. **8 kol z 82**, kdy měl kouč míč v dosahu a **šel jinam** — je tam šance
   opravdu mizerná, nebo je pokuta příliš tvrdá?
2. Proč se **klec sejde jen v 11 kolech z 303**.
3. **Delší běh**, aby čistota rohů měla vzorek, ze kterého se dá rozhodovat.
